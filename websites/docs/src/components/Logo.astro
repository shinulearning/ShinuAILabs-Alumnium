---
const { src, alt, className, href } = Astro.props;
---

<a href={href}>
  <img
    class:list={[
      "max-h-14 dark:brightness-0 dark:invert dark:opacity-50",
      className,
    ]}
    src={src}
    alt={alt}
    title={alt}
  />
</a>
