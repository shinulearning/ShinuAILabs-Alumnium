---
import { isBareStarlightPage } from "#/utils/starlight";
import Default from "@astrojs/starlight/components/PageTitle.astro";

const isBare = isBareStarlightPage(Astro.locals);
---

{
  !isBare && (
    <Default {...Astro.props}>
      <slot />
    </Default>
  )
}
