---
import { isBareStarlightPage } from "#/utils/starlight";
import Default from "@astrojs/starlight/components/ContentPanel.astro";

const isBare = isBareStarlightPage(Astro.locals);
---

{
  isBare ? (
    <div class="content-panel bare-layout">
      <div class="sl-container">
        <slot />
      </div>
    </div>
  ) : (
    <Default {...Astro.props}>
      <slot />
    </Default>
  )
}

<style>
  .bare-layout .sl-container {
    max-width: none;
  }
</style>
