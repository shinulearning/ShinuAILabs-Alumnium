---
title: Alumnium v0.14 with a standalone server and an agent to find elements
pubDate: 2025-10-03
description: Alumnium introduces a client-server architecture with Docker support and a new agent for finding elements with natural language.
author:
  name: Alex Rodionov
  image: alex_rodionov.jpeg
tags: ["release notes"]
---

Alumnium [v0.14][1] establishes the foundation for multi-language support and improves developer experience. Highlights include a standalone server with RESTful API, Docker containerization, and a natural language agent for finding UI elements.

## Standalone Server

This release introduces [a client-server architecture][2] that centralizes LLM interactions on a server component. The server provides REST API endpoints for all core Alumnium capabilities including session management, area identification, step execution, verification, data retrieval, and planning.

By moving LLM interactions to the server, there will be only one implementation of Alumnium prompts, agents, and supported models. This enables client implementations in multiple languages beyond Python, making Alumnium accessible to a broader developer ecosystem.

## Docker Image

The [server][3] is now available at `alumnium/alumnium` on Docker Hub, enabling containerized deployments across different platforms. The image packages the Alumnium server for use by clients implemented in other languages.

You can launch the container with:

```bash
docker run --rm -p 8013:8013 \
    --volume $(PWD)/.alumnium/cache:/app/.alumnium/cache \
    --env-file .env \
    alumnium/alumnium
```

## Element Finding Agent

A new [agent for finding elements][4] enables developers to locate UI components using natural language queries and get native framework instances:

```python
submit_button = al.find("submit button")
submit_button.click()
```

The agent accepts a text description and returns it in a format compatible with the underlying automation framework—either Appium/Selenium's `WebElement` or Playwright's `Locator`. This provides a simpler alternative to complex selector chains while maintaining full compatibility with existing automation code.

## Coming Next

The client-server architecture opens the door to TypeScript, Java, and other language implementations. We're excited to see the Alumnium ecosystem expand to support more development workflows and testing frameworks.

[1]: https://github.com/alumnium-hq/alumnium/releases/tag/0.14.0
[2]: https://github.com/alumnium-hq/alumnium/pull/147
[3]: https://github.com/alumnium-hq/alumnium/pull/165
[4]: https://github.com/alumnium-hq/alumnium/pull/164
